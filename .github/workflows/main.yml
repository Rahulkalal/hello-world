name: Trigger on Branch Patterns for Push

on:
  push:
    branches:
      - 'master'     # Trigger on push to the main branch
      - 'develop'  # Trigger on push to the develop branc

jobs:
  echo-hello-world:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello-world - Runner OS $RUNNER_OS"
